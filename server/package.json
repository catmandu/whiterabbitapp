{
  "name": "white-rabbit-app-server",
  "version": "1.0.0",
  "description": "backend app for white rabbit app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "frontend-build": "cd ../client && npm install && npm run build",
    "backend-build": "npm install",
    "build": "npm run frontend-build && npm run backend-build",
    "dev": "nodemon server.js"
  },
  "author": "catmandu",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^2.2.0",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongoose": "^8.15.0",
    "nodemon": "^3.1.10"
  }
}
